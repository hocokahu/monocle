---
context: default
status: paused
branch: hoc/claude-skill
last_updated: 2026-04-20T18:22:07.432Z
---

<current_state>
Working on Claude Code instrumentation for Monocle on branch `hoc/claude-skill`.
This session: removed `workflow.claude_code.subagent` span type (subagent internal spans now nest
directly under parent workflow), changed `entity.1.name` default from "general-purpose" to "sub-agent",
and added Table of Contents + "What Do We Want Answered" section + Line 252 full JSON example to HOOK_PLAN.md.
Two uncommitted files: `transcript_processor.py` and `HOOK_PLAN.md`.
All 4 integration tests pass.
</current_state>

<completed_work>

Previous sessions:
- Merged `main` into `hoc/claude-skill`, resolved Gemini streaming conflict
- Added `user.name` (from git config) to workflow and turn spans
- Renamed `MONOCLE_SERVICE_NAME` → `MONOCLE_WORKFLOW_NAME`
- Bumped apptrace version to `0.7.7+claudecli`
- Created `/kahu` skill, tested against production
- Updated `/ok-add-framework` skill with turn→invocation hierarchy
- Pulled PR #1 from mohammed: token aggregation fix, DELEGATES_TO telemetry
- Added sub-agent JSONL discovery and span emission (commit 5f7a1209)
- Added real JSONL-to-spans worked example in HOOK_PLAN.md (commit 00dc6dd5)
- Investigated mid-turn queue-operation visibility and sub-agent JSONL linkage

This session:
- Removed `workflow.claude_code.subagent` span type entirely from `process_subagents`
  — subagent turns/inference/tools are now direct children of parent workflow span
- Changed `entity.1.name` default to `"sub-agent"` when `subagent_type` not explicitly provided
  (was defaulting to `"general-purpose"` which is just a code fallback, not meaningful metadata)
- Span name follows same logic: `"Sub-Agent: Explore"` when type explicit, `"Sub-Agent"` when default
- Added Table of Contents to HOOK_PLAN.md
- Added "What Do We Want Answered by Instrumenting Claude CLI?" section (21 questions, 6 categories)
- Added Line 252 full JSON example with explanation (first streaming snapshot = inference call, thinking is empty)
</completed_work>

<remaining_work>

- Commit this session's changes (transcript_processor.py + HOOK_PLAN.md)
- Correlate subagent→parent via `parentToolUseID` (subagent spans share trace but lack explicit link to the Agent tool_use span that spawned them)
- Verify `monocle_apptrace.version: 0.7.7+claudecli` appears in actual traces
- Capture mid-turn queue-operation messages in spans (requires JSONL scanning in Stop hook, no hook event available)
</remaining_work>

<decisions_made>

- Removed `workflow.claude_code.subagent` entity type — no separate workflow span for subagents; internal spans are children of parent workflow, distinguishable by `scope.agentic.session`
- `entity.1.name` defaults to `"sub-agent"` when `subagent_type` not explicitly in tool input — avoids showing "general-purpose" which was just a code default
- Sub-Agent span name: `"Sub-Agent: {type}"` when type explicit, `"Sub-Agent"` when default
</decisions_made>

<blockers>
- None currently
</blockers>

## Required Reading (in order)
1. `apptrace/src/monocle_apptrace/instrumentation/metamodel/claude_code/transcript_processor.py` — main processor with subagent changes
2. `examples/scripts/HOOK_PLAN.md` — design doc with new ToC, motivation section, and Line 252 example
3. `apptrace/src/monocle_apptrace/instrumentation/metamodel/claude_code/_helper.py` — helper with SubagentInfo, classify_tool, entity type constants

## Infrastructure State
- Branch `hoc/claude-skill` at `22dea683` + uncommitted changes
- `monocle_apptrace 0.7.7+claudecli` installed locally in editable mode
- Stop hook running via `.claude/settings.local.json` — confirmed active in logs
- Okahu production: UP and working
- All 4 integration tests pass

<next_action>
Start with: Commit the two modified files, then either implement parentToolUseID correlation or verify traces in production.
</next_action>
