---
context: default
status: paused
branch: hoc/claude-skill
last_updated: 2026-04-14T12:40:00Z
---

<current_state>
Working on Claude Code instrumentation for Monocle on branch `hoc/claude-skill`.
Merged main into branch. All code changes committed and pushed.
Two uncommitted items: `/kahu` skill and its cached state file.
</current_state>

<completed_work>

Session work:
- Merged `main` into `hoc/claude-skill`, resolved Gemini streaming conflict
- Added `user.name` (from git config) to workflow and turn spans (no git CLI dependency — reads config files directly)
- Removed `user.email` (PII concern)
- Renamed `MONOCLE_SERVICE_NAME` → `MONOCLE_WORKFLOW_NAME` with `DEFAULT_WORKFLOW_NAME` env var fallback
- Bumped apptrace version to `0.7.7+claudecli` and reinstalled
- Verified via Okahu MCP that `user.name: "hocokahu"` appears in traces (trace `0xa9d0cbbdb76472b134229b0ec28002c6`)
- Created `/kahu` skill — queries Okahu SRE Agent API with auto-env detection
- Tested `/kahu` against production successfully
</completed_work>

<remaining_work>

- Commit `/kahu` skill (`.claude/commands/kahu.md` and `.claude/state/kahu_env.json`)
- Verify `monocle_apptrace.version: 0.7.7+claudecli` appears in new traces (staging was down, couldn't confirm)
- Update `.env` with production OKAHU_API_KEY if desired (currently has staging key)
- Consider adding `.claude/state/` to `.gitignore` (contains cached env detection)
</remaining_work>

<decisions_made>

- Read git config files directly (configparser) instead of shelling out to `git` CLI — works even without git installed
- `user.email` removed — PII concern, only `user.name` captured
- Version format `0.7.7+claudecli` — PEP 440 local version label (allows alphanumeric after `+`)
- `MONOCLE_WORKFLOW_NAME` env var with `DEFAULT_WORKFLOW_NAME` fallback, matching Monocle's workflow naming pattern
- `/kahu` validates API key once per key prefix, caches in `.claude/state/kahu_env.json`
- Validation uses lightweight `GET /api/v1/sessions` endpoint (no agent call)
</decisions_made>

<blockers>
- Okahu staging API (`api-stage.okahu.co`) was timing out all session — couldn't verify new traces on staging
- `.env` has staging API key (`okh_TvXJNYyn_...`) which only works on staging; production key is `okh_SzokcBME_...`
</blockers>

## Required Reading (in order)
1. `examples/scripts/claude_code_hook/monocle_hook.py` — the Stop hook with user.name and WORKFLOW_NAME changes
2. `apptrace/src/monocle_apptrace/instrumentation/metamodel/claude_code/transcript_processor.py` — span emission with user.name
3. `.claude/commands/kahu.md` — the /kahu skill definition

## Infrastructure State
- Branch `hoc/claude-skill` pushed to `origin` (hocokahu/monocle)
- `monocle_apptrace 0.7.7+claudecli` installed locally in editable mode
- Okahu staging: DOWN (504/timeout)
- Okahu production: UP and working

<next_action>
Start with: Commit the `/kahu` skill, then verify traces show `0.7.7+claudecli` version once staging recovers.
</next_action>
