---
session: claude-code-monocle-framework
status: committed-and-pushed
last_updated: 2026-04-03T05:41:08.651Z
---

# Session Handoff: Claude Code Monocle Framework

## Current State

All code committed and pushed to `hoc/claude-skill` as `hocokahu` (commit `dc306267`).
Framework package, hook scripts, tests, and docs all in place. Traces verified in Okahu Stage via MCP.
Package installed as editable (`pip install -e apptrace/`).

## Completed Work

- Framework package: `apptrace/src/.../metamodel/claude_code/` (4 files)
- Registration: `span_handler.py` + `wrapper_method.py` (2 files modified)
- Hook scripts: `examples/scripts/claude_code_hook/` (repo-local, not pip-installed)
- Integration tests: `apptrace/tests/integration/test_claude_code.py` (4 tests, all pass)
- E2e tests: 3 tests emitting to Okahu, all verified via Okahu MCP
- Design doc: `examples/scripts/HOOK_PLAN.md` with mapping tables
- Deleted unused `entities/` subfolder (was dead code)
- Moved hooks from `~/.claude/hooks/` to repo-local paths
- Committed (dc306267) and pushed to `origin/hoc/claude-skill`

## Key Decisions

- No `entities/` folder — span attributes built inline in `transcript_processor.py`
- `SimpleSpanProcessor` not `BatchSpanProcessor` — hook is short-lived
- Hook at project level (`examples/scripts/`) not pip-installed, like Langfuse pattern
- Framework package (`_helper.py`, `transcript_processor.py`) is pip-installed via `monocle_apptrace`

## Remaining Work

- [ ] Test with real Claude Code session (live hook, not simulated transcripts)
- [ ] Handle MCP tool spans (`mcp__*` prefix) in e2e testing
- [ ] PR to monocle2ai/monocle main branch

## Environment

```
OKAHU_API_KEY=okh_TvXJNYyn_PLj0x0qXRcyafFXRydgA
OKAHU_INGESTION_ENDPOINT=https://ingest-stage.okahu.co/api/v1/trace/ingest
MONOCLE_EXPORTER=okahu,file
pip install -e apptrace/  (editable install required)
```

Okahu Stage: https://okahu-portal-ui-stage-eus.azurewebsites.net/en/components/claude-cli

## Next Action

Test with a real Claude Code session — trigger the Stop hook and verify live traces appear in Okahu.

## Resume

```
Read .continue-here.md
Use /ok:resume or /ok:add-framework to continue
```
