# Continue Here — Monocle Claude Code Hook

**Paused:** 2026-04-20
**Branch:** `hoc/claude-skill`

## Session Summary

This session completed two features and one investigation:

### 1. HOOK_PLAN.md Real Example (DONE)
- Added "## Real Example: JSONL to Spans" section with real trace `0xfc8ac4b80f9a8ed695f83887a431e853`
- Shows 7 JSONL lines → streaming merge → 3 Monocle spans (inference + Bash + Grep)
- Commit: `00dc6dd5`

### 2. Sub-Agent JSONL Processing (DONE)
- `_helper.py`: Added `SubagentInfo`, `discover_subagents()`, `read_subagent_jsonl()`
- `transcript_processor.py`: Added `process_subagents()`, renamed `"Tool: Agent"` → `"Sub-Agent: {type}"`, added `entity.1.model`
- `monocle_hook.py`: Calls `discover_subagents()` + `process_subagents()` after parent transcript, tracks `subagents_processed` in state
- `HOOK_PLAN.md`: Updated Agent span table, marked Phase 2 checklist done with hierarchy diagram
- Commit: `5f7a1209`
- All 4 integration tests pass

### 3. Investigation Findings

**Mid-turn user messages (`queue-operation`):**
- JSONL type `queue-operation` with `operation: "enqueue"/"remove"` captures messages sent while Claude is working
- NO hook event fires for these — no `QueueOperation` or `MidTurnMessage` hook
- They get folded into the ongoing turn as `system-reminder` injections
- The hook's `data.input` only captures the first user message that starts the turn
- **Gap remains**: mid-turn messages are invisible to spans

**Sub-agent JSONL structure:**
- Located at `{session-id}/subagents/agent-{agent-id}.jsonl` + `.meta.json`
- Parent JSONL also has `type=progress` lines with `parentToolUseID` containing subagent messages inline
- `.meta.json` has `{"agentType": "Explore", "description": "..."}`
- Correlation: `parentToolUseID` in progress lines links to parent Agent `tool_use.id`

## Remaining Work

### Open items from HANDOFF.json (from Apr 16):
- [ ] Verify `monocle_apptrace.version 0.7.7+claudecli` in traces (staging was down)
- [ ] Update `.env` with production `OKAHU_API_KEY` if desired

### Phase 2 remaining:
- [ ] Correlate subagent→parent via `parentToolUseID` (subagent spans share trace but lack explicit parent link to the Agent tool_use span)

### Future considerations:
- [ ] Capture mid-turn `queue-operation` messages (scan JSONL for `type: "queue-operation"` in Stop hook)
- [ ] Process subagents even when no new parent messages exist (edge case: subagent finishes after parent turn already processed)

## Key Decisions

- Sub-agent workflow spans are children of the parent workflow span (shared trace_id via OTel context nesting), not children of the specific Agent tool_use span
- `read_subagent_jsonl()` filters to user/assistant messages only — skips `progress` and `file-history-snapshot` entries
- Agent span renamed from "Tool: Agent" to "Sub-Agent: {type}" since `span.type` is `agentic.invocation` not `agentic.tool.invocation`

## Working Tree

Clean — all changes committed and pushed.
